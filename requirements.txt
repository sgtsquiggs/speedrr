# Pinned deliberately: this fork exists because the unpinned versions of these
# packages drifted into incompatibility. See README.md ("qBittorrent 5.x fork").
#
#   qbittorrent-api  <2025.11.0 rejects qBittorrent 5.2+ logins. 5.2.0 changed the
#                    WebAPI to answer 204 with an empty body where it used to answer
#                    200 "Ok."; older releases compare the body to "Ok." and raise
#                    LoginFailed on an authentication that actually succeeded.
#   dataclass_wizard 1.0.0 moved YAMLWizard out of the top-level namespace, so
#                    helpers/config.py fails to import against it. 0.39.1 is the
#                    last 0.x release.
#
# Versions below resolved and tested together on python:3.10 (the Dockerfile base).
dataclass_wizard==0.39.1
PyYAML==6.0.3
qbittorrent-api==2026.8.1
transmission-rpc==7.0.12
httpx==0.28.1
colorama==0.4.6
